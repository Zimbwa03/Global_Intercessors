import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.globalintercessors.app',
  appName: 'Global Intercessors',
  webDir: 'dist/public'
};

export default config;
